{
  "rules": {
    "products": {
      ".read": true,
      "$id": { ".write": "auth != null && auth.token.email == 'nayemahamed776@gmail.com'" }
    },
    "orders": {
      "$id": {
        ".write": "auth != null && auth.token.email == 'nayemahamed776@gmail.com' || (!data.exists() && newData.exists())",
        ".read": "auth != null && auth.token.email == 'nayemahamed776@gmail.com'"
      }
    },
    "tracking": {
      "$id": { ".read": true, ".write": "auth != null && auth.token.email == 'nayemahamed776@gmail.com' || (!data.exists() && newData.exists())" }
    },
    "reviews": {
      "$id": { ".read": true, ".write": "auth != null && auth.token.email == 'nayemahamed776@gmail.com' || (!data.exists() && newData.exists())" }
    }
  }
}
